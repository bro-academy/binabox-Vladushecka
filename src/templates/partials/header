 <header class="header">
        <a class="logo" href="#">Binabox</a>
        <nav class="nav">
            <ul class="nav__list list-item">
                {% for item in nav %}
                <li>
                    <a href="{{item.url}}"
                    class="main-nav__link"
                    {% if item.url !=path %}
                    main-nav__link--active
                    >{{item.text}}</a>
                </li>
                {% endfor %}
                <li class="list-item__link"><a href="#">Home</a></li>
                <li class="list-item__link"><a href="#">About</a></li>
                <li class="list-item__link"><a href="#">Collection</a></li>
                <li class="list-item__link"> <a href="#">Fags</a></li>
                <li class="list-item__link"><a href="#">Pages</a></li>
            </ul>
        </nav>
        <div>
            <input type="checkbox">
            <a class="button--primary" href="/home">Discord</a>
            <a class="button" href="/home">Connect</a>
        </div>
    </header>