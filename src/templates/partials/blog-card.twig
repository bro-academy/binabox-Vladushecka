 <article class="blog-card">
                <span class="blog-card__label" for="nft">{{ card.label }}</span>
                <img class="blog-card__img" id="nft" src="{{ card.scr }}" alt="{{ card.alt }}">
                <time class="blog-card__time" datetime="{{ card.datetime }}">{{ card.time }}</time>
                <h3 class="blog-card__autor">{{ card.title }}</h3>
                <p class="blog-card__description">{{ card.text }}</p>
                <a class="blog-card__button" href="{{ card.href }}">Read more</a>
            </article>