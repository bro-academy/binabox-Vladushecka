  <a class="card" href="#">
                <article>
                    <span class="card__label" for="new">{{ item.label}}</span>
                    <img class="card__img" id="new" src="{{ item.src }}" alt="{{ item.alt }}">
                    <p class="card__price">{{ item.price }}</p>
                    <h3 class="card__header">{{ item.name }}</h3>
                </article>
            </a>